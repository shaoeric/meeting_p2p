# Checklist

## 双实例启动
- [x] `npm run test:local` 一键启动两个独立 Electron 窗口
- [x] `npm run test:a` 单独启动实例A
- [x] `npm run test:b` 单独启动实例B
- [x] 两个实例使用独立的 `userData` 目录（.test-data/a 和 .test-data/b）

## 实例隔离
- [x] 两个实例的存储数据互不可见
- [x] 关闭一个实例不影响另一个实例运行
- [x] 两个实例可同时使用麦克风（独立音频流）

## 测试流程
- [x] `.test-data/README.txt` 存在且包含完整测试步骤
- [x] 测试步骤覆盖：创建房间 → 加入房间 → 语音通话 → 屏幕共享 → 标注协作 → 结束会议
- [x] 测试步骤中明确标注"在实例A 中操作"和"在实例B 中操作"

## P2P 通信验证
- [ ] 实例A 创建房间后，实例B 能通过房间号成功加入（需实际运行双实例验证）
- [ ] 两个实例之间实现实时语音通话（需实际运行双实例验证）
- [ ] 屏幕共享画面能在两个实例间传输（需实际运行双实例验证）
- [ ] 标注操作能在两个实例间实时同步（需实际运行双实例验证）